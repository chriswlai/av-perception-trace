from typing import Dict, List


def run_detectors(image_path: str) -> List[Dict[str, object]]:
    """
    Placeholder for model inference. Should return a list of detections.
    Each detection is a dict with keys like: id, label, bbox, confidence.
    """
    raise NotImplementedError("Connect to a detector or nuScenes annotations.")
